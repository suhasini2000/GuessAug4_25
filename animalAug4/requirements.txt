# Core Django
Django>=5.0,<6.0

# Database (PostgreSQL recommended)
psycopg2-binary>=2.9.9

# Django REST Framework (API for React frontend)
djangorestframework>=3.15.0

# CORS handling (for React <-> Django requests)
django-cors-headers>=4.3.1

# Authentication / JWT (if using token auth)
djangorestframework-simplejwt>=5.3.1

# Environment variable management
python-decouple>=3.8

# Pillow for image handling (if needed)
Pillow>=10.3.0

# Other useful packages
gunicorn>=22.0.0   # for deployment
whitenoise>=6.7.0  # for serving static files in production
